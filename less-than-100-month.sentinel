import "tfrun"
import "decimal"

delta_monthly_cost = decimal.new(tfrun.cost_estimate.delta_monthly_cost)

if delta_monthly_cost.greater_than(1000) {
    print("Spend greater than $1000 not approved for Dev Resoources.")
}

main = rule {
    delta_monthly_cost.less_than(1000)
}
